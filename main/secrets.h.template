// Copy this file to secrets.h and fill in your credentials.
#pragma once
#define WIFI_SSID       "YOUR_SSID"
#define WIFI_PASS       "YOUR_PASSWORD"
// Where the device fetches the new firmware binary from.
// E.g. "https://github.com/<user>/<repo>/releases/download/v1.0.1/firmware.bin"
#define OTA_URL         "https://example.com/firmware.bin"
